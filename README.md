# Java Spring Boot Service

A simple Spring Boot REST API project for DevOps portfolio.

## Features
- REST endpoint: `/users`
- In-memory H2 database
- Unit tests with JUnit
- GitHub Actions CI workflow

## Run locally
```bash
mvn spring-boot:run
```

Then open: [http://localhost:8080/users](http://localhost:8080/users)
